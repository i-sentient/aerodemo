import { TarsMount } from './TarsMount'

// ---------------------------------------------------------------------------
//  SCENE 3 · "ICU continued" — picks up EXACTLY where Scene 2 ended: the TARS
//  ICU app. No ward-tour re-run — we land straight in the ICU interface, which
//  is where the post-PCI continuation content lives. Terminus of the chain.
// ---------------------------------------------------------------------------
export function Root() {
  return <TarsMount />
}
